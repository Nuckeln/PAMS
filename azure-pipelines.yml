# Python package
# Create and test a Python package on multiple Python versions.
# Add steps that analyze code, save the dist with the build record, publish to a PyPI-compatible index, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/python

trigger:
- master

pool:
  vmImage: ubuntu-latest
strategy:
  matrix:
    Python310:
      python.version: '3.10'

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '$(python.version)'
  displayName: 'Use Python $(python.version)'

- script: |
    python -m venv antenv
    source antenv/bin/activate
    python -m pip install --upgrade pip
    pip install --target="./antenv/lib/python3.10/site-packages" -r ./requirements.txt
  displayName: 'Install dependencies'

- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: '$(Build.SourcesDirectory)'
    includeRootFolder: false
    archivetype: 'zip'
    archiveFile: '$(Build.ArtifactStagingDirectory)/Application$(Build.BuildId).zip'
    replaceExistingArchive: true
    verbose: true # (no value); this input is optional
    
- publish: $(Build.ArtifactStagingDirectory)/Application$(Build.BuildId).zip
  displayName: 'Upload Package'
  artifact: drop

  # Clean Deployment auf Azure Web App
- task: AzureWebApp@1
  displayName: 'Deploy to Azure Web App (Clean)'
  inputs:
    azureSubscription: 'YourServiceConnectionName'  # <- Hier deinen Service Connection Namen eintragen
    appName: 'WA-PD-NE-CMES-REPORTING-SUPERDEPOTUI-dev'
    package: '$(Build.ArtifactStagingDirectory)/Application$(Build.BuildId).zip'
    deploymentMethod: 'auto'
    takeAppOfflineFlag: true  # Erzwingt Bereinigung alter Inhalte